#include <stdio.h>

int main() {
    int a, b, sum;
    printf("Enter a number: ");
    scanf("%d", &a);
    printf("Enter a number: ");
    scanf("%d", &b);
    sum = a + b; 
    printf("%d", sum);
    return 0;
}